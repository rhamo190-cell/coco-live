// This file is intentionally left as a placeholder.
// The app uses a custom tab layout in app/(main)/_layout.tsx
// instead of the scaffold's default (tabs) structure.
export default function TabLayout() {
  return null;
}
